/**
 * `landingCarouselSection` uses the same CMS projection and card mapping as
 * `locationCarouselSection` (cities / resolved manual items → carousel).
 * Kept as a separate registry key to match Sanity section type naming.
 */
export { locationCarouselSectionHandler as landingCarouselSectionHandler } from './locationCarouselSection'
