import * as React from "react";
import SeoText from "@/components/landing/sections/impl/SeoTextSectionImpl";

export function SeoTextSection(props: React.ComponentProps<typeof SeoText>) {
  return <SeoText {...props} />;
}

