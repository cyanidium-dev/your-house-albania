/**
 * The two messenger entry points of the business, in one place.
 *
 * They are shown on /about, written into the privacy policy's operator line and
 * emitted as Organization `contactPoint` URLs. Several features import this
 * module, so the export names are a contract: do not rename them.
 */
export const BUSINESS_WHATSAPP_URL = "https://wa.me/message/KPXIGD5DJISGO1";
export const BUSINESS_TELEGRAM_URL = "https://t.me/real_estate_al";

/** General contact address; the same one the privacy policy gives. */
export const BUSINESS_EMAIL = "hello@domlivo.com";
