/**
 * General contact address; the same one the privacy policy gives.
 *
 * Kept apart from `businessContacts.ts`, which is shared with other branches
 * and exports exactly the two messenger URLs.
 */
export const BUSINESS_EMAIL = "hello@domlivo.com";
